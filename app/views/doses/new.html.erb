<div class="container">
  <div class="row">
    <%= simple_form_for [@cocktail, @dose] do |f| %>
      <%= f.input :description %>
      <%= f.association :ingredient,collection: Ingredient.all(order: 'name') ,default: Ingredient.find(1) %>
      <%= f.submit %>
    <% end %>
  </div>
</div>
